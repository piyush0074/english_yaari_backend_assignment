# Server
PORT=3000
NODE_ENV=development
LOG_LEVEL=debug

# Route prefix for all feature APIs (set to empty to expose bare paths like /users)
API_PREFIX=/api

# CORS: comma-separated origins, or * for any
CORS_ALLOWED_ORIGINS=*

# MongoDB
MONGO_URI=mongodb://127.0.0.1:27017/session-booking
